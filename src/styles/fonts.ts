import { rem } from "@utils/style";

export const fontsSizes = {
	s: rem(0.8), // 12px,
	m: rem(1), // 16px,
	l: rem(2), // 32px,
	lx: rem(3), // 48px,
	lxx: rem(4), // 64px,
};

export const fontWeights = {
	regular: 400,
	medium: 500,
	bold: 700,
};
